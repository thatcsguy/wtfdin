## MODAL-002: Custom Rename Modal
- Replaced browser prompt() with styled HTML modal for character rename
- Added input-modal HTML structure (title, input field, Cancel/Confirm buttons)
- Added CSS: .modal-small, .modal-input, .modal-buttons, .btn-secondary
- Added JS: openInputModal(), closeInputModal(), handleInputModalConfirm()
- Modal supports Enter to submit, Escape to cancel, click outside to close
- Input pre-filled with current name, auto-focused and selected
- Files changed: index.html
- Screenshot: .playwright-mcp/rename-modal.png

Note: MODAL-001 already passing - melee DPS (6 jobs) fits in one row at 600px modal width

## MODAL-003: Custom Delete Confirmation Modal
- Replaced browser confirm() with styled HTML modal for character delete
- Added confirm-modal HTML structure (title, message paragraph, Cancel/Delete buttons)
- Added CSS: .btn-danger, .modal-message
- Added JS: openConfirmModal(), closeConfirmModal(), handleConfirmModalConfirm()
- Modal shows character name, supports Escape to cancel, click outside to close
- Delete button auto-focused for accessibility
- Reusable for future confirm dialogs (job remove, etc.)
- Files changed: index.html

## MODAL-004: Custom Job Remove Confirmation Modal
- Updated handleRemoveJob() to use openConfirmModal() instead of browser confirm()
- Modal title: "Remove Job", button text: "Remove"
- Shows job name in confirmation message
- Reuses existing confirm modal infrastructure from MODAL-003
- Files changed: index.html

## MODAL-005: Custom Add Character Modal
- Updated handleAddCharacter() to use openInputModal() instead of browser prompt()
- Added confirmText param to openInputModal() for dynamic button text
- Modal title: "Add Character", button text: "Create"
- Reuses input-modal infrastructure from MODAL-002
- Files changed: index.html

## BIS-002: Toggle Slot Type on Gear Page
- Made slot-type badge clickable to toggle between raid/tome
- Added slotTypeOverrides to job data structure for per-slot customization
- Added getSlotType() helper to resolve override vs preset
- CSS: cursor:pointer, hover effects on .slot-type badges
- Checkbox state resets when type changes (gear[slot] = {})
- Updated validateRingChange() and calculateJobProgress() to use overrides
- Persists via localStorage
- Files changed: index.html

## RAID-001: Raid Turn Display
- Added RAID_TURNS mapping: slot → {turn, label}
- Weapon=4th, Head/Hands/Feet=2nd, Body/Legs=3rd, Accessories=1st
- Turn label displays next to Raid badge in Type column
- Added .turn-label CSS (smaller font, muted color)
- Label appears/disappears when toggling slot type
- Files changed: index.html

## TOME-001: Tomestone Cost Display
- Added TOME_COSTS mapping: slot → cost
- Weapon=500, Body/Legs=825, Head/Hands/Feet=495, Accessories=375
- Cost label displays next to Tome badge in Type column with "tomes" suffix
- Added .tome-cost CSS (smaller font, muted color, ::after content for "tomes")
- Reused extraLabel pattern from RAID-001, renamed turnLabel → extraLabel
- Files changed: index.html

## BIS-001: BiS Configuration Step
- Two-step job add flow: step1=job picker, step2=BiS config
- Step 2 shows two buttons: "Use Preset BiS" or "Configure Manually"
- Manual config shows all 11 slots with Raid/Tome toggle buttons
- Added pendingJobId + manualSlotConfig state for modal
- New functions: showJobModalStep(), handleJobModalBack(), showBisConfigStep(), addJobWithConfig()
- Only stores slotTypeOverrides if manual config differs from preset
- CSS: .bis-config-step, .bis-option-buttons, .bis-option-btn, .bis-slot-grid, .bis-slot-item, .bis-slot-toggle
- Back button returns to step 1, Cancel closes modal
- Files changed: index.html

## SUMMARY-001: Raid Drops Summary Panel
- Added summary panel to right of gear table
- Added calculateRaidDropsByTurn() - counts unchecked raid slots by turn
- Panel shows "Raid Drops Needed" section with turns grouped (1st-4th)
- Each turn shows drop count, e.g. "1st Turn: 2 drops"
- Turns with 0 drops are hidden
- Shows "All raid drops obtained!" when complete
- Real-time updates on checkbox changes
- CSS: .gear-layout (flex), .gear-table-container, .summary-panel, .summary-section, .summary-item
- Mobile: panel moves above table
- Files changed: index.html
